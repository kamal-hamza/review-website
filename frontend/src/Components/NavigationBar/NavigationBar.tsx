import { Navbar, Container } from "react-bootstrap";
import styles from "./NavigationBar.module.css";

function Navigationbar() {
    return (
        <Navbar>
            <Navbar.Brand>Review</Navbar.Brand>
        </Navbar>
    );
}

export default Navigationbar;